[build]
buildCommand = "pnpm turbo build"

[deploy]
startCommand = "cd packages/database && (node_modules/.bin/prisma migrate resolve --applied 20260505000000_init 2>&1 || true) && node_modules/.bin/prisma migrate deploy && cd ../.. && pnpm --filter web start"
