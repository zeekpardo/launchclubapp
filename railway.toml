[build]
buildCommand = "pnpm turbo build && cd packages/database && (npx prisma migrate resolve --applied 20260505000000_init 2>&1 || true) && npx prisma migrate deploy"

[deploy]
startCommand = "pnpm --filter web start"
